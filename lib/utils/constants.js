/**
 * The minimum number of color palettes required for valid input.
 */
const MIN_REQUIRED_PALETTES = 2

/**
 * The minimum number of tokens required for each color mode.
 */
const REQUIRED_TOKENS = 5

export {
  MIN_REQUIRED_PALETTES,
  REQUIRED_TOKENS
}